{
  "$schema": "https://schema.railpack.com",
  "provider": "python",
  "build": {
    "commands": ["pip install -r requirements.txt"]
  },
  "deploy": {
    "startCommand": "gunicorn --bind :$PORT backend:app --workers 1 --timeout 120"
  }
}